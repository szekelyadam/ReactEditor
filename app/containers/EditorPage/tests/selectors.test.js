// import { fromJS } from 'immutable';
// import { makeSelectEditorPageDomain } from '../selectors';

// const selector = makeSelectEditorPageDomain();

describe('makeSelectEditorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
